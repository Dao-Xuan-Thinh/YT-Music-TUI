yt-dlp>=2024.1.0
textual>=0.47.0
mutagen>=1.47.0
ytmusicapi>=1.12.1
requests>=2.28
# OS media keys + now-playing panel (optional — the app runs without them)
pyobjc-framework-MediaPlayer>=10.0 ; sys_platform == 'darwin'
dbus-fast>=2.0 ; sys_platform == 'linux'
winrt-runtime>=3.0 ; sys_platform == 'win32'
winrt-Windows.Media>=3.0 ; sys_platform == 'win32'
winrt-Windows.Media.Playback>=3.0 ; sys_platform == 'win32'
winrt-Windows.Foundation>=3.0 ; sys_platform == 'win32'
