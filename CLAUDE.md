# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## This branch: `mobile-fork` — the native iOS app only

This branch contains the **native iOS port** of YouTube Music TUI: a SwiftUI app with an
embedded CPython running `yt-dlp` on-device (no server, nothing downloaded to disk).
The **desktop Python/Textual app lives on `master`** (dev branch: `test`) together with
its docs (`CLAUDE.md`, `IMPROVEMENTS.md`, `YOUTUBE_LOGIN.md`). Don't edit desktop code
here — it was removed from this branch.

`NATIVE_REWRITE.md` is the original design doc (architecture decisions, milestone log).
`ios/README.md` covers build/run details.

## Layout

```
ios/project.yml            ← XcodeGen spec → YTMusic.xcodeproj (generated, gitignored)
ios/build.sh               ← build+run: `./build.sh sim` | `./build.sh device TEAMID [devid]`
ios/fetch-deps.sh          ← downloads Python.xcframework + vendors yt-dlp/ytmusicapi etc.
ios/Support/               ← Python.xcframework (gitignored, ~146MB) + VERSIONS pin list
ios/app/resolve.py         ← ALL Python entry points: resolve/search/home/artist/album/
                             playlist/lyrics/translate/radio/auth check (JSON in/out;
                             every function returns {_ok/ok:…} and never raises)
ios/app/ios_jsc_provider.py← pure-Python yt-dlp JS-challenge provider via ctypes→JavaScriptCore
ios/app_packages/          ← vendored pure-Python deps (gitignored; fetch-deps.sh)
ios/YTMusic/*.swift        ← SwiftUI app (see roles below)
ios/YTMusic/PythonBootstrap.{h,m} ← embedded-CPython init + python_* C bridge functions
```

Swift file roles: `YTMusicApp` (entry) · `ContentView` (root: tabs, search, library,
For-You, queue + all sheet/cover presentation) · `PlayerViewModel` (central state:
queue, resolve pipeline, prefetch, lyrics/translate, radio, artist/collection pages) ·
`PlaybackService` (AVPlayer + lock-screen/now-playing + `AudioLevelTap` equalizer via
MTAudioProcessingTap) · `NowPlayingScreen` (full-screen player: seek, lyrics follow,
radio/shuffle/repeat) · `ArtistScreen` / `CollectionScreen` (artist page, album/playlist
view) · `AccountScreen`+`AccountStore` (WKWebView/paste sign-in → ytmusicapi browser
auth) · `SettingsScreen`, `Theme`/`ThemePickerSheet` (TUI-style themes + color-wave) ·
`LibraryStore` (liked/playlists/recent persistence) · `Track`/`Artist` (models).

## Build & run

```sh
brew install xcodegen                 # one-time
cd ios && ./fetch-deps.sh             # one-time / when deps change
./build.sh sim                        # simulator
./build.sh device YK4NZ9U7TL 034EFB07-1E60-5107-A97D-BE9686A0CAEA   # iPhone
```

Device facts (free Apple ID — 7-day signing, auto-provisioned via
`-allowProvisioningUpdates`):
- Team ID `YK4NZ9U7TL` (Dao Xuan Thinh, Personal Team).
- iPhone 16 Pro "PRX F0resk1n 😛": `034EFB07-1E60-5107-A97D-BE9686A0CAEA`
- iPad Pro 11" M5 ":333": `64B0EE2D-917D-56A3-A4F7-F30848A26BBB`
- Install/launch (needs full Xcode, not CommandLineTools):
  `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun devicectl device install app --device <id> build/Build/Products/Debug-iphoneos/YTMusic.app`
  then `… device process launch --console --device <id> com.ytmtui.YTMusic`.
- One-time on each device: enable Developer Mode + trust the dev profile.

## Critical gotchas

- **Single-arch builds only** (`ARCHS=arm64 ONLY_ACTIVE_ARCH=YES`, baked into build.sh):
  the xcframework's `install_python` post-build phase assumes one arch (`lib-$ARCHS`).
- **`ios/YTMusic/Info.plist` is generated by xcodegen** — gitignored; never commit it.
- **Python bridge pattern:** every `python_*()` call happens on a background queue
  (GIL serializes them — a hung call starves the rest, so `resolve.py` sets
  `socket_timeout=15, retries=1` on yt-dlp and PlayerViewModel debounces selection
  ~150 ms + runs a 30 s resolve watchdog). Results are C strings the Swift side must
  `free()`. Never call the bridge on the main thread.
- **Lyrics must use an anonymous ytmusicapi client**: `get_lyrics(timestamps=True)` goes
  through `as_mobile()` (ANDROID_MUSIC), which rejects cookie/browser auth with HTTP 400.
  `resolve.py:lyrics()` builds a fresh anonymous `YTMusic()` for lyrics only.
- **Sheet/cover presentation:** SwiftUI cannot present two sibling
  `fullScreenCover`s from the same view. All root-level covers live in ContentView; a
  cover that must appear *on top of* another presented screen must be attached *inside*
  that screen's view (see ArtistScreen → CollectionScreen).
- **JS challenges without JIT:** iOS bans Deno/Node; `ios_jsc_provider.py` registers a
  yt-dlp `JsChallengeProvider` running the EJS solver through JavaScriptCore's C API via
  ctypes. Often unneeded (`android_vr` client returns m4a directly) but required as
  fallback.
- Embedded Python `print()` is invisible; surface diagnostics via NSLog / returned JSON.
  On-device logs: `devicectl … launch --console`.
- The Mac is chronically low on disk; device builds need the ~8.5 GB iOS platform
  runtime present in Xcode (re-fetch: `xcodebuild -downloadPlatform iOS`).

## Conventions

- Track dict shape is desktop-compatible: `{id, title, uploader, duration, url, thumbnail}`.
- No emojis in UI — Unicode symbols only (∞ ♪ ♥ ✕ ▸ ◆ ◇ etc.).
- Commit as the user's git identity, **without** a Claude co-author trailer.
